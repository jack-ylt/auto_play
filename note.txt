
git status
git add .
git commit -m
git push

git push origin master
git push origin multi_client
git push origin login_in_turn
git push --delete origin branch_name_here

git branch
git checkout

git log
                


★ 如何实现一键游戏，做到完全省心？
    图像识别准确
    每个逻辑都独立，并覆盖所有情况
    每个任务都可以配置，做到遵循玩家意愿
    结束后，有个报告，告知任务完成情况，让玩家放心
    即使出现问题，也不影响别的
    出现预料之外的情况，有记录，可追溯
    出现游戏bug，无响应等异常，能重启游戏


开发中遇到的阻碍：
    很多情况没有考虑到
    -》 伪代码

    截图太麻烦
    -> 截有特色的, 使用好的截图软件

    api记不住
    -> Shpinx 自动生成项目文档

    代码框架感觉有点混乱了
    -> 重新理一下



★ eye能够自己保存截屏img，过期了就重新截一个，没过期就还用原来的。
不要让player还去考虑并发，公用截屏img的问题。
至于target_img可以使用global_value来缓存

关于配置，可以直接copy default的，取名game_account.cfg，然后用户想改的话，可以自己改


为了能支持快速停止，所有耗时函数，都需要是协程



TODO
    
    ★ 鼠标双击，经常失灵。
    =》找了半天原因，原来是360导致的。关掉后，又快又好。
    
    
    物品买了，就变灰色，图像识别到了，又点击怎么办？
    =》
    1 点了，不弹出确认框，就认为买过了
    
    2 加入颜色验证
    pyautogui.pixelMatchesColor(100, 200, (140, 125, 134), tolerance=10)
    True
    

鼠标点击失灵
    -> 双击失灵，卸载360，解决了
    -> 单击偶尔也失灵
        -> 管理员身份运行
        -> 先move_to再click
        
    家园boos，有些特殊的位置就是点不了
    =》改成精确点boos图标
    
    
log 要分开打印
上层要捕获异常，不能让程序直接崩溃
    def make_logger(name):
        handler = logging.FileHandler(filename=name + ".log")
        logger = logging.getLogger(name)
        logger.addHandler(handler)
        logger.setLevel(logging.INFO)
        return logger
    logger = make_logger("a")

    auto_play, player_eye, hand 都用player的logger （使用属性，传参的方式公用）
    player的logger根据角色名或者窗口名创建
    其它用main的logger


游戏名字，任务名字，用中文拼音更好。


每次在一个新设备登陆，傻白都要跑出来，烦死了
-> 一直点傻白，等它没了，就点退出（esc无效）


★ 不要考虑各种特殊情况，如果不符合就报错退出。比如：
- 没有配置account.cfg
    - 配置错误，比如有重复的section （提示用户参照文档，重新配置）
- 有的窗口，游戏没有安装齐全


★ 是否可以做一个检查，检查环境是否都ok，ok就可以流畅运行程序。
否则，报告问题点，让用户改。


★ 要考虑网络问题了，要确保一切都在计划内。
比如： 点击A，then monitor B，这个B最好是当前界面没有的


★ 出现了一次，鼠标完全失灵。无法esc，无法点击，也不能move
导致程序一直在跑，但游戏一动不动。
但手动可以，手动点击了一下之后，程序恢复了
-> 原因: press后，没有release
# 试试pyautogui
-> autogui也是有各种问题，试试管理员运行

自动驻军
    闪烁的方框，就是可以进攻的
    如何区分盟友和敌人呢？
        点击后，看防守方 （需要人为设置）
        
    -》 手动输入坐标，然后自动驻军


提高效率：
    很多task，完成了，就没必要在玩了（包括task中的小task）
    极速模式config
    缓存所有pics的可能位置
    timeout=0
    每个task自己退出到主界面

log要按角色分，这样才方便定位分析

久游登入的时候，可能有广告，特殊关闭按钮    done

★ 鼠标点击失灵，还是没有解决
-> 预兆：鼠标移动不了了，让它去点击a，实际上它还是停在b不动
-> 所以，可以在click函数中做一个verify

有时候真的是点一次没用，需要再点一次


要有一气呵成的操作，否则可能忙不过来
比如：点太慢，切换账号失败


写个自动合成5星、6星，9星的程序

★ 如果已经登录好了，开启脚本，就只完成这三个任务，不要再切账号了（应对久游验证码）

★ gold2 包含了按钮花纹，而按钮花纹会因为位置不同而不同，影响识别率

★ counter改成数据库存储，是否会更好？比如：
玩家名，任务名，属性名，属性值，修改日期，有效期
这样的话，数据的增删改查会更容易


TODO:
邀请英雄，增加每周买光暗3星		done
自动打高塔试炼	done
冠军试炼优化	done  ★（因为需要额外安装软件，默认不要开启）
	poor 要看积分
	冠军试炼，用到了一个文字识别的功能，需要用到这个软件：
	tesseract

	可以通过以下链接下载安装：
	https://digi.bib.uni-mannheim.de/tesseract/
按照用户配置顺序来执行游戏，如果2小时内完成过，则跳过它		done
战斗界面，有时候点击失灵，或者未识别到按钮，可能会浪费4、5分钟	done
公会战boos，偶尔还是会消耗钻石	done
有时候游戏会卡死，点任何地方都没反应，需要重启游戏
按空格暂停游戏，按esc退出游戏
末世军团更新为了末日前线，需要新开发一个登陆程序	done
高塔试炼，周一上午处于冷却器，打不了	done
购买光暗3星，啤酒比不足		done

写个自动合成5星、6星，9星的程序
	合成5星
		遣散 1 2
		
		while true
			获取空余格子
			获取现有3 4 数量
			按比例添加3星、4星
			快速合成
			统计5星数量
			if > 20:
				break
				
	合成6星
		
		
	

	

